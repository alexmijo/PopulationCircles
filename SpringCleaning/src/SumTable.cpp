#include <vector>

template <typename T>
class SumTable {
public:
    SumTable(const std::string& sumTableBinaryFilename) {
// TODO
    }

    int width;
    int height;
private:
    std::vector<std::vector<T>> sumTable;
};

